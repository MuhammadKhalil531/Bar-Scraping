{
  "name": "bot-deduction",
  "version": "1.0.0",
  "description": "Puppeteer scraper for Bravsearch (PrimeFaces) — bar filter, pagination, JSON/CSV export",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node src/index.js",
    "merge-csv": "node scripts/merge-csvs.js",
    "scrape": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "puppeteer": "^24.0.0"
  }
}
